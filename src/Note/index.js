import React from "react";
import { StyledNote } from "./styled";

const Note = (props) => (
    <StyledNote>
        Pola z oznaczeniem * wymagają wypełnienia
    </StyledNote>
)

export default Note;